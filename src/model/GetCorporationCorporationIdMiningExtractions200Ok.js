/*
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * OpenAPI spec version: 1.12
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.28
 *
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['ApiClient'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    module.exports = factory(require('../ApiClient'));
  } else {
    // Browser globals (root is window)
    if (!root.EveApi) {
      root.EveApi = {};
    }
    root.EveApi.GetCorporationCorporationIdMiningExtractions200Ok = factory(root.EveApi.ApiClient);
  }
}(this, function(ApiClient) {
  'use strict';

  /**
   * The GetCorporationCorporationIdMiningExtractions200Ok model module.
   * @module model/GetCorporationCorporationIdMiningExtractions200Ok
   * @version 1.12
   */

  /**
   * Constructs a new <code>GetCorporationCorporationIdMiningExtractions200Ok</code>.
   * 200 ok object
   * @alias module:model/GetCorporationCorporationIdMiningExtractions200Ok
   * @class
   * @param chunk_arrival_time {Date} The time at which the chunk being extracted will arrive and can be fractured by the moon mining drill. 
   * @param extraction_start_time {Date} The time at which the current extraction was initiated. 
   * @param moon_id {Number} moon_id integer
   * @param natural_decay_time {Date} The time at which the chunk being extracted will naturally fracture if it is not first fractured by the moon mining drill. 
   * @param structure_id {Number} structure_id integer
   */
  var exports = function(chunk_arrival_time, extraction_start_time, moon_id, natural_decay_time, structure_id) {
    this.chunk_arrival_time = chunk_arrival_time;
    this.extraction_start_time = extraction_start_time;
    this.moon_id = moon_id;
    this.natural_decay_time = natural_decay_time;
    this.structure_id = structure_id;
  };

  /**
   * Constructs a <code>GetCorporationCorporationIdMiningExtractions200Ok</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/GetCorporationCorporationIdMiningExtractions200Ok} obj Optional instance to populate.
   * @return {module:model/GetCorporationCorporationIdMiningExtractions200Ok} The populated <code>GetCorporationCorporationIdMiningExtractions200Ok</code> instance.
   */
  exports.constructFromObject = function(data, obj) {
    if (data) {
      obj = obj || new exports();
      if (data.hasOwnProperty('chunk_arrival_time'))
        obj.chunk_arrival_time = ApiClient.convertToType(data['chunk_arrival_time'], 'Date');
      if (data.hasOwnProperty('extraction_start_time'))
        obj.extraction_start_time = ApiClient.convertToType(data['extraction_start_time'], 'Date');
      if (data.hasOwnProperty('moon_id'))
        obj.moon_id = ApiClient.convertToType(data['moon_id'], 'Number');
      if (data.hasOwnProperty('natural_decay_time'))
        obj.natural_decay_time = ApiClient.convertToType(data['natural_decay_time'], 'Date');
      if (data.hasOwnProperty('structure_id'))
        obj.structure_id = ApiClient.convertToType(data['structure_id'], 'Number');
    }
    return obj;
  }

  /**
   * The time at which the chunk being extracted will arrive and can be fractured by the moon mining drill. 
   * @member {Date} chunk_arrival_time
   */
  exports.prototype.chunk_arrival_time = undefined;

  /**
   * The time at which the current extraction was initiated. 
   * @member {Date} extraction_start_time
   */
  exports.prototype.extraction_start_time = undefined;

  /**
   * moon_id integer
   * @member {Number} moon_id
   */
  exports.prototype.moon_id = undefined;

  /**
   * The time at which the chunk being extracted will naturally fracture if it is not first fractured by the moon mining drill. 
   * @member {Date} natural_decay_time
   */
  exports.prototype.natural_decay_time = undefined;

  /**
   * structure_id integer
   * @member {Number} structure_id
   */
  exports.prototype.structure_id = undefined;


  return exports;

}));
